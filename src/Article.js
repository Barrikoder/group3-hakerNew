function Article(props) {
  return (
    <h1>{props.article.author}</h1>
    /* <p>
        {hits.author} {tags.comment}
      </p>
      <div>{user.email}</div>
      <div>{user.phone}</div> cmnd k + cmnd c */
  );
}
export default Article;
